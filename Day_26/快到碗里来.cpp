#include <iostream>
#include <string>

using namespace std;

long double r_to_c(long double r){
  long double c = 0;//周长
  
}

void Compare_n_r(string n, string r){
  if()
}
 
int main() {
  string n,r;
  while(cin >> n >> r){
    if(n.size() >  r.size())
      cout << "Yes" << endl;
    else if(n.size() - r.size() > 2)
      cout << "No" << endl;
    else
      Compare_n_r(n,r);

  }
  return 0;
}
